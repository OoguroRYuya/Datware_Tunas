# URL koneksi database PostgreSQL (contoh: Aiven, RDS, Local PostgreSQL)
# Ganti username, password, host, port, dan dbname sesuai dengan kredensial Anda.
DATABASE_URL=postgresql://username:password@host:port/dbname?sslmode=require
